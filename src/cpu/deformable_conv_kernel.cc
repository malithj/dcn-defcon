#include <deformable_conv_kernel.h>

template <typename T>
void deformable_conv_kernel(T *offsets, T *input, index_t height, index_t width,
                            index_t kernel_height, index_t kernel_width,
                            index_t in_channels, index_t out_channels,
                            index_t batches) {}